'use client';

import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { ArrowRight } from 'lucide-react';

export function NewProjectForm() { const router = useRouter(); const [title, setTitle] = useState(''); const [storytellerName, setStorytellerName] = useState(''); const [storytellerRelationship, setStorytellerRelationship] = useState(''); const [preferredLanguage, setPreferredLanguage] = useState('en'); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); async function submit(event: React.FormEvent) { event.preventDefault(); setBusy(true); const response = await fetch('/api/projects', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ title, storytellerName, storytellerRelationship, preferredLanguage }) }); const body = await response.json(); if (!response.ok) { setError(body.error ?? 'Please check the form.'); setBusy(false); return; } router.push(`/projects/${body.project.id}`); router.refresh(); } return <form className="panel paper form-stack" onSubmit={submit}><div className="field"><label htmlFor="title">What would you like to call this story?</label><input id="title" value={title} onChange={(event) => setTitle(event.target.value)} placeholder="Maya's Story" required /></div><div className="field"><label htmlFor="storytellerName">Who is the storyteller?</label><input id="storytellerName" value={storytellerName} onChange={(event) => setStorytellerName(event.target.value)} placeholder="Maya Sharma" required /></div><div className="field"><label htmlFor="relationship">How are they connected to you?</label><input id="relationship" value={storytellerRelationship} onChange={(event) => setStorytellerRelationship(event.target.value)} placeholder="Grandmother" required /></div><div className="field"><label htmlFor="language">Preferred language</label><select id="language" value={preferredLanguage} onChange={(event) => setPreferredLanguage(event.target.value)}><option value="en">English</option><option value="ne">Nepali</option><option value="en-ne">English + Nepali</option></select></div>{error && <div className="error-box">{error}</div>}<button className="button primary" disabled={busy}>{busy ? 'Creating…' : 'Create project'} <ArrowRight size={16} /></button></form>; }
