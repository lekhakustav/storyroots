import { currentUser, bad, guardRateLimit, ok } from '@/lib/api';
import { addRecording, getInterview } from '@/lib/store';

const allowed = new Set(['audio/webm', 'audio/mpeg', 'audio/mp3', 'audio/mp4', 'audio/x-m4a', 'audio/wav', 'audio/x-wav']);
export async function POST(request: Request, context: { params: Promise<{ sessionId: string }> }) { const user = await currentUser(); if (!guardRateLimit(user.id, 'upload', 20)) return bad('Please wait before uploading again.', 429); const found = getInterview(user, (await context.params).sessionId); if (!found) return bad('Interview not found.', 404); if (!found.project.consent?.recordingConsent) return bad('Recording consent is required before uploading audio.', 403); const form = await request.formData(); const file = form.get('file'); if (!(file instanceof File)) return bad('Please choose an audio file.'); if (file.size > 50 * 1024 * 1024) return bad('Audio files must be smaller than 50 MB.'); if (file.type && !allowed.has(file.type)) return bad('Use a webm, mp3, m4a, or wav file.'); const recording = addRecording(user, found.interview.id, { originalFilename: file.name || 'interview-audio', mimeType: file.type || 'audio/webm', durationSeconds: Number(form.get('durationSeconds') || 0) || null, fileSize: file.size }); return recording ? ok({ recording, provider: 'mock-storage' }, { status: 201 }) : bad('Could not save recording.', 500); }
