import { AppShell } from '@/components/app-shell';
import { getCurrentUser } from '@/lib/auth';
export default async function SettingsPage() { const user = await getCurrentUser(); return <AppShell user={user} title="Settings"><div className="content"><div className="page-heading"><div><div className="eyebrow">Your account</div><h1>Settings</h1><p className="muted">Keep your account and family data under your control.</p></div></div><div className="grid-2"><div className="panel"><h2>Account</h2><p><strong>{user.fullName}</strong><br />{user.email}</p><div className="action-row"><form action="/api/auth/logout" method="post"><button className="button secondary">Log out</button></form></div></div><div className="panel"><h2>Data deletion</h2><p>Project deletion removes its recordings, transcripts, chapters, and exports. This local demo keeps data only while the app is running.</p><button className="button danger" disabled>Delete my data</button></div></div></div></AppShell>; }
